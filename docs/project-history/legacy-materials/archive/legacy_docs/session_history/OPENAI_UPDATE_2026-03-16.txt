This update converts the repository into a locally testable morphology-aware field intelligence stack without replacing the numerical core. I normalized packaging around src/mycelium_fractal_net, kept legacy api/analytics imports as thin compatibility wrappers, and kept mfn as the canonical CLI. I also implemented real detect, forecast, compare, and report commands on top of the existing simulation and feature-extraction pipeline, added a practical Makefile workflow, and verified install/import/CLI/test execution locally. Proof executed here: editable install, mfn --help, mfn simulate/extract/detect/forecast/compare/report, make report, and pytest on smoke + CLI workflow subsets. Generated sample artifacts are included under artifacts/local.
