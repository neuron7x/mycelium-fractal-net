# MyceliumFractalNet Dependencies
# This file is synchronized with pyproject.toml [project.dependencies]
# For development dependencies, use: pip install -e ".[dev]"

# Core dependencies
numpy>=1.24
sympy>=1.12
torch>=2.0.0
pydantic>=2.0

# Data processing
pandas>=1.5.3,<3.0.0
pyarrow>=10.0.0

# Cryptography
# Provides production-grade cryptographic operations:
# - AES-256-GCM encryption for sensitive data
# - Digital signatures (Ed25519, RSA, ECDSA)
# - X.509 certificate handling
# - FIPS-compliant implementations via OpenSSL backend
cryptography>=44.0.0

# API & Web
fastapi>=0.100.0
websockets>=12.0

# Monitoring & Observability
prometheus_client>=0.17.0
