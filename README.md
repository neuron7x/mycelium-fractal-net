
# MyceliumFractalNet v4.1 — Neuro-Fractal Mycelium Dynamics Engine

**Стан:** 28 листопада 2025  
**Мова:** Python 3.10+  
**Автор:** Ярослав Василенко (neuron7x) + GPT-5.1 (ко-інженер)

---

## 🎯 Суть проєкту

MyceliumFractalNet v4.1 — це інженерно акуратна, відтворювана реалізація "нейро-міцеліальної" системи, яка:

- моделює потенціали мембрани через **рівняння Нернста**;
- еволюціонує поле потенціалів у вигляді **дифузійної решітки**, схожої на міцелій;
- оцінює **фрактальну розмірність** конфігурації через box-counting;
- запускає короткий цикл навчання невеликої нейромережі (**MyceliumFractalNet**) по синтетичним характеристикам поля;
- видає метрики стабільності, відсутності NaN/OOM, діапазони потенціалів, фрактальну розмірність.

Проєкт оформлено як **повний GitHub-репозиторій**: код, тести, документація, CI-пайплайн, pre-commit, типізація.

---

## 🧱 Структура репозиторію

```text
MyceliumFractalNet_v4_1_full/
├── pyproject.toml
├── requirements.txt
├── .gitignore
├── .pre-commit-config.yaml
├── mycelium_fractal_net_v4_1.py        # CLI-ентрі по специфікації v4.1
├── src/
│   └── mycelium_fractal_net/
│       ├── __init__.py
│       └── model.py                    # Уся фізика, фрактали, валідація
├── tests/
│   ├── test_nernst.py
│   ├── test_fractal_dimension.py
│   ├── test_validation_cycle.py
│   └── test_determinism.py
├── docs/
│   ├── ARCHITECTURE.md
│   ├── VALIDATION_NOTES.md
│   └── ROADMAP.md
└── .github/
    └── workflows/
        └── ci.yml
```

---

## 🚀 Швидкий старт (локально)

### 1. Встановити залежності

```bash
pip install -r requirements.txt
# або повний dev-набір
pip install -e ".[dev]"
```

### 2. Запустити валідацію (режим "seed=42, CPU")

```bash
python mycelium_fractal_net_v4_1.py --mode validate --seed 42 --epochs 1 --batch-size 2
```

Очікуваний результат (діапазони, не магічні числа):

- **drop loss**: початковий > фінального, принаймні на 20–30 %;
- **фрактальна розмірність D**: в межах `1.4 ≤ D ≤ 1.9`, std невеликий;
- **потенціали мембрани**: в діапазоні ~ `[-95, 40] мВ`, без NaN;
- **growth_events**: ≥ 1;
- відсутність NaN/OOM, стабільний runtime.

### 3. Прогнати тести

```bash
pytest -q
```

Запустяться:

- аналітичні тести для рівняння Нернста;
- тести для оцінки фрактальної розмірності;
- інтеграційний тест валідаційного циклу;
- тест детермінізму при фіксованому seed.

---

## 🧪 Метрики та гарантії (інженерний рівень)

У цьому артефакті **немає "заглушок"**: усі функції реально працюють, усі тести виконують обчислення.

Гарантується:

- **Фізична коректність Нернста:**
  - для [K⁺]_in = 140 mM, [K⁺]_out = 5 mM при 37°C очікується E_K ≈ -89 мВ;
- **Фрактальний аналіз:** реалізовано box-counting-оцінку розмірності для бінарного поля;
- **Стабільність дифузійної решітки:** обмеження потенціалів та регулярний лапласіан;
- **Детермінізм:** при однаковому seed результат метрик повторюваний в рамках floating-point похибки.

---

## 🧰 Інструменти інженерної якості

- **Тести:** `pytest`, `pytest-cov`;
- **Статичний аналіз:** `ruff`, `mypy`;
- **Форматування / стиль:** `black`, `isort` (через pre-commit);
- **CI/CD:** GitHub Actions workflow `ci.yml` для `push` і `pull_request`:
  - встановлення залежностей;
  - запуск `ruff`, `mypy`;
  - запуск `pytest` з coverage.

---

## ⚙️ Використання в інших проєктах

Ви можете:

- встановити пакет у режимі editable та імпортувати:
  ```python
  from mycelium_fractal_net import (
      compute_nernst_potential,
      simulate_mycelium_field,
      estimate_fractal_dimension,
      run_validation,
  )
  ```
- вбудувати симуляцію міцелію як **штучний "нейро-поле"** у MLSDM/TradePulse/інші системи;
- розширювати архітектуру `MyceliumFractalNet` своїми шарами / loss-функціями.

---

## 📎 Ліцензія

MIT (можна публічно викладати на GitHub, форкати, розширювати, продавати обгортки/продукти поверх цього артефакту).
